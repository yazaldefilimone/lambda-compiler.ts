import { Type } from "types";

export const contextTabeleTypes:Map<string, Type> = new Map<string, Type>();
export type ContextTabeleTypes = typeof contextTabeleTypes;
export const contextTabeleNormalize:Map<string, Type> = new Map<string, Type>();
export type ContextTabeleNormalize = typeof contextTabeleNormalize;